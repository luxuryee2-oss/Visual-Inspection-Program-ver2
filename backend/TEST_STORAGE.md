# 저장 기능 테스트 가이드

## 현재 상태

✅ 서버가 정상적으로 실행 중입니다!
✅ 루트 레벨 라우트 작동 확인

## 다음 단계: 저장 기능 테스트

### 1단계: 진단 엔드포인트 확인

브라우저에서 환경 설정 확인:

```
http://localhost:3000/api/inspection/health-check
```

이 URL에서 다음을 확인하세요:
- ✅ 모든 환경 변수가 설정되어 있는지 (`hasAzureClientId`, `hasSharePointSiteUrl` 등)
- ✅ Azure 인증 상태

### 2단계: 실제 저장 테스트

#### PC에서 테스트
1. 프론트엔드 실행 (다른 터미널):
   ```bash
   cd frontend
   npm run dev
   ```

2. 브라우저에서 프론트엔드 접속
3. 검사 데이터 입력 및 저장 시도

#### 또는 API 직접 테스트

Postman이나 curl로 직접 테스트:

```bash
curl -X POST http://localhost:3000/api/inspection \
  -H "Content-Type: application/json" \
  -d "{
    \"productName\": \"테스트제품\",
    \"inspector\": \"홍길동\",
    \"notes\": \"테스트 비고\",
    \"photos\": {
      \"front\": null,
      \"back\": null,
      \"left\": null,
      \"right\": null
    }
  }"
```

### 3단계: 서버 콘솔 확인

저장 시도 시 **서버 콘솔**에 다음이 출력됩니다:

```
============================================================
📥 새로운 저장 요청 수신
============================================================
요청 데이터: {...}
✅ 유효성 검사 통과
🔄 SharePoint 저장 시작...
🔐 SharePoint 저장 함수 시작
...
✅ 저장 완료! (소요 시간: ... ms)
============================================================
```

### 4단계: 오류 확인

만약 저장이 실패하면 서버 콘솔에 상세한 오류 메시지가 표시됩니다:
- Azure 인증 오류
- SharePoint 연결 오류
- 리스트 저장 오류

## 문제 해결

### 환경 변수 미설정

`health-check` 엔드포인트에서 `false`로 표시되는 항목 확인:
- `hasAzureClientId: false` → `.env` 파일에 설정 필요
- `hasSharePointSiteUrl: false` → SharePoint URL 설정 필요

### Azure 인증 실패

서버 콘솔에 "인증 실패" 오류가 표시되면:
- `.env` 파일의 Azure 설정 확인
- 클라이언트 시크릿이 올바른지 확인

### SharePoint 저장 실패

서버 콘솔 로그를 확인하여 구체적인 오류 메시지 확인

## 다음 확인 사항

1. **진단 엔드포인트 결과** (`/api/inspection/health-check`)
2. **저장 시도 시 서버 콘솔 로그**
3. **오류 메시지** (있는 경우)

이 정보를 주시면 저장 문제를 정확히 진단하겠습니다!


