# 라우트 테스트 가이드

## 서버 재시작 후 테스트 순서

### 1. 서버 재시작
```bash
cd backend
npm run dev
```

서버 시작 시 콘솔에 다음이 출력되어야 합니다:
```
✅ 라우트 등록 완료: /api/inspection
📍 라우터 등록됨: ...
```

### 2. 기본 Health Check 테스트
```
http://localhost:3000/api/health
```
예상 응답: `{"status":"ok"}`

### 3. 간단한 테스트 라우트
```
http://localhost:3000/api/inspection/test
```
예상 응답: `{"message":"라우터 작동 중!","timestamp":"..."}`

이 라우트가 작동하면 → 라우터 자체는 정상

### 4. 진단 엔드포인트
```
http://localhost:3000/api/inspection/health-check
```

## 문제 진단

### /api/inspection/test가 작동하는데 health-check가 안 되는 경우
→ health-check 엔드포인트의 코드에 문제가 있을 수 있음
→ 서버 콘솔 로그 확인

### /api/inspection/test도 안 되는 경우
→ 라우터가 제대로 등록되지 않음
→ 서버 재시작 필요
→ 서버 콘솔에서 "라우트 등록 완료" 메시지 확인

### 모든 라우트가 안 되는 경우
→ 서버가 실행되지 않았거나 다른 포트에서 실행 중
→ 포트 3000 확인




